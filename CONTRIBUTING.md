# 协作指南

欢迎一起把《学生时代牌》做得更好。这份文档说明**怎么改、怎么提交、怎么不把游戏改坏**。

---

## 一、协作流程

### 分支模型

| 分支 | 用途 | 规则 |
|---|---|---|
| `main` | 稳定可玩版本 | 不直接推，只通过 Pull Request 合并 |
| `content/xxx` | 新增或调整游戏内容（卡牌、事件、怪物、剧情） | 从 `main` 开出 |
| `fix/xxx` | 修 bug | 从 `main` 开出 |
| `feature/xxx` | 新增系统或玩法 | 从 `main` 开出 |
| `art/xxx` | 纯素材替换与新增 | 从 `main` 开出 |

### 完整流程

```bash
git switch main && git pull                    # 1. 更新到最新
git switch -c content/新增运动会事件            # 2. 开分支
#                                          ...3. 改文件...
node tools/check-assets.mjs                    # 4. 提交前自检（必跑）
git add -A
git commit -m "content: 新增运动会系列事件"      # 5. 提交
git push -u origin content/新增运动会事件       # 6. 推送
```

7. 到托管平台网页上发起 **Pull Request**，说明改了什么、怎么验证的
8. 至少 **1 人** 审核通过后合并

### 提交信息规范

```
<类型>: <一句话说明>
```

| 类型 | 含义 |
|---|---|
| `content` | 游戏内容：卡牌、事件、怪物、剧情、数值 |
| `fix` | 修复缺陷 |
| `feat` | 新增系统或玩法 |
| `art` | 美术素材 |
| `audio` | 音频素材 |
| `ui` | 界面与版式 |
| `docs` | 文档 |
| `chore` | 杂项、配置 |

---

## 二、改内容的正确姿势

### 场景 1：调整已有数值 / 文案（推荐做法）

**不要**去改 `js/data.js`、`js/monsters-expanded.js` 这类原始数据文件，除非你确定要改的就是基底数据。

**推荐**写进 `js/editor-overrides.js`，它是覆盖层，冲突面小、回滚方便：

```js
// js/editor-overrides.js
(function(){
  const E = G.EVENTS;
  if (!E) return;
  const set = (id, i, patch) => {
    if (E[id] && E[id].opts && E[id].opts[i]) Object.assign(E[id].opts[i], patch);
  };
  set('notebook', 0, { text: '归还失主', eff: 'int1', effDesc: '智力+1' });
})();
```

这个文件里已经有大量同类写法，照抄格式即可。

### 场景 2：新增卡牌 / 事件 / 怪物

直接在对应的 `js/*-expanded.js` 里追加条目，保持现有数据结构一致。

> 建议先在小分支上试，跑通游戏确认没问题再提 PR。

### 场景 3：用编辑器改

编辑器适合**试错和快速调整**（战斗界面布局、事件选项、开局剧情都有可视化编辑）。

注意层归属：

- 编辑器里改完 → 存在浏览器本地（`localStorage`），**别人看不到**
- 想贡献出去 → 点「**写入游戏**」生成 `editor-overrides.js`，或点「**导出全部**」拿到 JSON 后，整理进 `js/editor-overrides.js` 再提交
- 「导出全部」得到的 `金榜题名-编辑器数据.json` 是个人备份，**不要提交**（已在 `.gitignore` 中排除）

---

## 三、素材规范

### 放哪

| 类型 | 目录 |
|---|---|
| 角色立绘、Q 版头像 | `立绘/` |
| UI 按钮、面板、图标 | `UI/`、`按钮素材/` |
| 卡牌外框 | `卡牌样式/` |
| 卡牌插画 | `卡牌插画/` |
| 事件框、背景、节点图 | `其他插画/` |
| 社交界面素材 | `社交/` |
| 音乐 / 音效 | `音乐/` |
| 视频演出 | `动态/` |

### 命名

- 沿用现有命名风格（`btn_xxx_normal.png`、`bg_xxx.png`、`img_xxx.png`）
- **不要用空格**，不要用 `（1）`、`副本`、`final` 这类后缀
- 替换素材时**保持文件名不变**，直接覆盖，这样代码不用改

### 体积红线

- 单个文件 **超过 5 MB** 请先压缩（PNG 可用 pngquant，音频优先 OGG 而非 WAV）
- 视频 / 音频走 Git LFS，新增大媒体文件不用手动配置，`.gitattributes` 已覆盖常见扩展名

### ⚠️ 引用素材后必须自检

这是本项目**真实踩过的坑**：曾经有事件框引用了不存在的图片路径，导致背景一直显示为纯黑，排查了很久。

```bash
node tools/check-assets.mjs
```

它会把 HTML/JS 里引用的所有资源路径扫一遍，报告缺失文件和编码异常。

---

## 四、代码风格

沿用现有风格，不要引入构建工具或框架：

- 用 `IIFE` 或直接脚本块，全局对象统一挂到 `window.G`
- 注释写中文，说明**为什么**这么做，而不是复述代码在做什么
- 新增文件若被 HTML 引用，记得在 `学生时代牌-网页版.html` 里按依赖顺序补上 `<script>`
- 不要格式化整份文件（会产生巨量无意义 diff），只改你需要的部分

---

## 五、提交前自检清单

- [ ] 游戏能正常启动，能进主菜单、能开一局
- [ ] `node tools/check-assets.mjs` 无报错
- [ ] 改动只涉及自己的功能范围，没有夹带无关文件
- [ ] 新增素材文件名规范、无空格、无"副本"
- [ ] 没有提交 `*.zip`、`金榜题名-编辑器数据.json` 等生成物
- [ ] 涉及玩家可见的改动，已想好要不要写进 `更新说明.txt`

---

## 六、遇到问题

- **游戏白屏 / 素材 404** → 跑 `node tools/check-assets.mjs`，多半是资源路径引用错了
- **同事的音乐视频没声音** → 本地执行 `git lfs install && git lfs pull`
- **存档异常** → 游戏存档在浏览器 `localStorage`，清除后重开；这是本地数据，不会进仓库
- **改了没生效** → 检查是不是改在了编辑器的本地覆盖层里（只影响你自己，且会覆盖你的代码改动）
